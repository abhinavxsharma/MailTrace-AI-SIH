# Python
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/

# Environment / secrets
.env
.env.*
!.env.example

# Node
node_modules/
dist/
build/
.vite/

# Logs / local data
*.log
*.sqlite
*.db

# ML model files (never commit model weights)
ml/models/*/model.safetensors
ml/models/**/*.safetensors
ml/models/**/*.bin
ml/models/**/*.pt
ml/models/**/*.onnx

# OS
.DS_Store

# IDE
.idea/
.vscode/

# Google OAuth credentials — NEVER commit these files
# Download credentials.json from GCP Console; set path via GOOGLE_OAUTH_CLIENT_SECRETS_FILE
credentials.json
token.json
*.token
*.token.json

# Local OAuth token cache directory (dev only)
backend/.secrets/
.secrets/

# Gmail connector dev artefacts
gmail_token*/
